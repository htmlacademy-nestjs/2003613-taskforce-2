export const enum AuthUser {
  Exists = 'User with this email exists already',
  NotFound = 'User not found',
  WrongPassword = 'User password is wrong',
}

export const MAX_EXECUTOR_OCCUPATIONS_LENGTH = 5;
